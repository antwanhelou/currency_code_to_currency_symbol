name: currency_code_to_currency_symbol
description: "Currency code to currency symbol"
version: 0.0.4
homepage: https://github.com/antwanhelou/currency_code_to_currency_symbol.git

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:


